{
  "dependencies": {
    "@lumaai/luma-web": "^0.2.2",
    "cropperjs": "^2.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix web-src",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  }
}
